import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
    mainContainer: {
      display: 'flex',
      alignItems: 'center',
      color: 'rgba(0, 0, 0, 0.5)',
    },
    smMargin: {
      margin: 1,
    },
    actionDiv: {
      textAlign: 'center',
    },
}));